export interface CharactersModel {
  episode?: [];
  gender: string;
  id: number;
  image: string;
  location?: { name: string; url: string };
  name: string;
  origin?: [];
  species: string;
  status: string;
  favourite?: boolean;
}
